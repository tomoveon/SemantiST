PROGRAM PLC_PRG
VAR_INPUT
    STR : STRING[STRING_LENGTH];
    SRC : STRING;
    REP : STRING;
END_VAR
VAR
    semantist_result : STRING[STRING_LENGTH];
END_VAR
semantist_result := REPLACE_CHARS(STR := STR, SRC := SRC, REP := REP);
END_PROGRAM
